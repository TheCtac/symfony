{%for data in data%}

<div class='show_tanks'>
	<div class='show_tanks_info'><font color='#fa0'>{{data.name}}</font><br>
		тип: <a href="{{path('acme_tanks_review',{'type':data.type})}}">{{data.type}}</a><br>
		рівень: <a href='tanks.php?tank_level_={{data.level}}'>{{data.level}}</a>
	</div>
		<a href="{{path('acme_tank',{'name':data.name} ) }}">
			<img width='100%' height='auto' src='{{asset(data.photo)}}'>
		</a>
</div>
{%endfor%}
